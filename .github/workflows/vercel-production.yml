# DEPRECATED — production runs on VPS (94.177.161.26 / felicepolesebarbershop.it).
# This workflow is disabled so pushes to main never redeploy to Vercel.
# Use Docker Compose on the VPS instead: docker compose down && docker compose up -d

name: Vercel production (disabled)

on:
  workflow_dispatch:
    # Manual only — do not auto-deploy to Vercel.

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Production is on VPS
        run: |
          echo "Vercel deploy is disabled. Production: https://felicepolesebarbershop.it (VPS 94.177.161.26)."
          exit 0
