POLESE BARBERSHOP
=================

Brand: FELICE POLESE — Barberia d'élite a Benevento
Sede: Corso Dante Alighieri, 44 – 82100 Benevento (BN)
Tel: +39 327 015 6225
C.F.: PLSFLC04S21A783K | P.IVA: 01894030624
Tutti i prezzi sono da intendersi IVA inclusa.

SITO LIVE
---------
  https://polesebarbershop.vercel.app
  Stack: Next.js (questo è il sito). Non usare vecchi file HTML statici.

AVVIO LOCALE
------------
  npm install
  Copia .env.example in .env.local (puoi lasciare vuoti Supabase/Resend)
  In locale, se non imposti nulla, l'accesso /gestionale è utente admin
  e password admin (default nel codice). Non committare .env.local.
  npm run dev
  Apri http://localhost:3000
  Gestionale: http://localhost:3000/gestionale
  Windows: doppio click su AVVIA.cmd

PRENOTAZIONI
------------
  Wizard 5 passi: Servizi → Barbiere → Data/ora → Dati + GDPR → Conferma.
  Listino ufficiale: CAPELLI / BARBA / COLORE (prezzi variabili «da X € a Y €»).
  Barbieri: Felice e Davide (poltrone indipendenti) + «Chiunque sia disponibile».
  Primo giorno prenotabile: max(oggi, 1 settembre 2026). Chiuso lunedì e domenica.
  Conferma sempre con file .ics (un solo VALARM: 30 minuti prima, niente
  avvisi a 1 giorno o 1 ora) anche se il database o Resend non sono ancora
  configurati. In quel caso il cliente vede un avviso e può scaricare il
  calendario; chiama il salone al +39 327 015 6225 per conferma.
  Disdetta: /appuntamento/[token] (DELETE). Lo slot si libera; il promemoria
  di 30 minuti non parte (ICS METHOD:CANCEL, senza VALARM).
  Email Resend a cliente + admin (quando RESEND_API_KEY è impostata).
  Nessun Twilio, QStash o WhatsApp come canale primario di prenotazione.
  Il pulsante WhatsApp e «Parla con il salone» sono contatto rapido
  (messaggio generico, senza consulenza specialistica).

GESTIONALE (CRM interno, stile YGITAL)
--------------------------------------
  URL: /gestionale  ( /admin reindirizza lì )
  Login: utente admin · password admin
  (oppure ADMIN_USER / ADMIN_PASSWORD se li imposti)
  Sessione cookie httpOnly, separata dal sito pubblico.
  Header del sito NON è il CRM: solo un link discreto «Gestionale» nel footer.

  Dashboard: anagrafica clienti (nome, telefono, email), servizi prenotati,
  visite (inclusi gli annullati, contrassegnati), ultima visita, spesa.
  Statistiche: visite per cliente, servizi più frequenti, tasso disdetta/no-show,
  incassi giorno/settimana (walk-in + prenotazioni).
  Agenda a colonne Felice | Davide + Walk-in.

  Notifiche gratis dal pannello:
  - Email via Resend se c'è RESEND_API_KEY, altrimenti errore in italiano.
  - WhatsApp senza Twilio: apre https://wa.me/<numero>?text= sul WhatsApp
    del barbiere (costo zero). Messaggi precompilati: promemoria, promo, follow-up.
    WhatsApp massivo: un link wa.me per cliente.

  Senza Supabase l'interfaccia resta usabile (stati vuoti).
  Indici opzionali: supabase/migrations/002_crm_indexes.sql

PAGINE LEGALI
-------------
  /privacy-policy
  /terms

COME ANDARE LIVE — 4 click (io non posso creare gli account al posto tuo)
------------------------------------------------------------------------
  Servono il tuo login Google/GitHub. Incolla le chiavi nel modulo che
  ti ho aperto qui, e le STESSE su Vercel. Piani gratuiti, niente Twilio.

  1. Supabase (database) — https://supabase.com
     Start your project → continua con GitHub.
     New project → nome polesebarbershop → region Frankfurt → Create.
     A sinistra SQL Editor → New query → incolla TUTTO
     supabase/migrations/001_schema.sql → Run.
     Ingranaggio (Project Settings) → API:
       Project URL          → SUPABASE_URL
       secret key (sb_secret_…) o JWT service_role → SUPABASE_SERVICE_ROLE_KEY
       Poi SQL Editor: 001_schema.sql e opzionale 002_crm_indexes.sql.

  2. Resend (email) — https://resend.com
     Sign up → API Keys → Create API Key → copia (inizia con re_).
     Fino a quando non verifichi polesebarbershop.it usa:
       RESEND_FROM=Polese Barbershop <beth.t@example.com>
     (con questo mittente di test le email arrivano solo a te)

  3. Accesso gestionale
     Default: utente admin e password admin.
     In produzione imposta ADMIN_USER e ADMIN_PASSWORD (almeno 4 caratteri).
     Email avvisi prenotazioni: ADMIN_EMAIL o OWNER_EMAIL (default felicepolese550@gmail.com)

  4. Vercel (sito live) — https://vercel.com
     progetto polesebarbershop → Settings → Environment Variables
     Incolla le stesse chiavi + NEXT_PUBLIC_SITE_URL=https://polesebarbershop.vercel.app
     Deployments → Redeploy.

  Senza questi 4 click la prenotazione resta attiva: conferma + file .ics,
  ma NON salva nel database e NON manda email. Chiama il +39 327 015 6225.

TEST
----
  npm test
  npm run build

CONTATTI
--------
  Tel / WhatsApp contatto: +39 327 015 6225
  Email salone: felicepolese550@gmail.com (non l'account GitHub)
  Instagram: @felicepolese_barber
  Indirizzo: Corso Dante Alighieri, 44 – 82100 Benevento (BN)
